rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /cache/{document} {
      // Cloud Functions write via Admin SDK (bypasses rules).
      // Frontend reads directly — public read, no writes.
      allow read: if true;
      allow write: if false;
    }
  }
}
